apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-torrent-tsocks.fullname" . }}-test-connection"
  labels:
{{ include "cloud-torrent-tsocks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cloud-torrent-tsocks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
