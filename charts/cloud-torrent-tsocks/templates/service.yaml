apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-torrent-tsocks.fullname" . }}
  labels:
    {{- include "cloud-torrent-tsocks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-torrent-tsocks.selectorLabels" . | nindent 4 }}
